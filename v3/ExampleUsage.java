package v3;
/*
public class ExampleUsage {
    public static void main(String[] args) {
        // Create a DicoDesReferencesCellules instance
        DicoDesReferencesCellules dicoCell = new DicoDesReferencesCellules();

        // Create Cellule instances
        Cellule cell1 = new Cellule("A1", dicoCell);
        Cellule cell2 = new Cellule("A2", dicoCell);

        // Create DependentCell instance
        DependentCell dependentCell = new DependentCell("B1", dicoCell);

        // Register dependentCell as an observer of cell1
        cell1.addObserver(dependentCell);

        // Set the formula for cell1
        cell1.setFormule("5");

        // Evaluate cell1, which will trigger the update in dependentCell
        cell1.evaluateCell();

        // At this point, you can access the updated value in dependentCell
        double dependentCellValue = dependentCell.getValeur();
        System.out.println("DependentCell value: " + dependentCellValue);
    }
}
*/